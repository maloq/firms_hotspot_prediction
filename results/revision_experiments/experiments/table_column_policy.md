# Table Column Policy

All CSV files under `experiments/*/tables/` are compact presentation tables with at most 8 columns. Raw wide outputs are archived as JSONL.GZ in `shared_artifacts/raw_tables_jsonl/` with schemas in `shared_artifacts/raw_table_schemas/`.
